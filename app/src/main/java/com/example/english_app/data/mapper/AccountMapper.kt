package com.example.english_app.data.mapper

import com.example.english_app.domain.model.Account
import com.google.firebase.firestore.auth.User

class AccountMapper {
    fun mapToDomain(user: User): Account {
        // Chuyển đổi đối tượng User từ lớp dữ liệu thành đối tượng User trong lớp domain
        TODO()
    }
}